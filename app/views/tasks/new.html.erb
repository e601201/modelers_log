<%= content_for(:title, t('tasks.new.title'))%>
<div class="mx-auto md:w-2/3 w-full">
  <%= tag.h1 t('.title'), class: "font-bold text-4xl p-4" %>
  <%= render "form",{ project: @project, task: @task }%>
</div>

<div class="tabs container mx-auto">
  <%= link_to t('workspaces.index.title'), workspace_path(@profile), class: 'tab tab-bordered' %>
  <%= link_to t('workspaces.profile.title'), workspace_profile_path(@profile), class: 'tab tab-bordered tab-active' %>
  <%= link_to t('workspaces.followings.index.title'), followings_path(@profile), class: 'tab tab-bordered'%>
  <%= link_to t('workspaces.followers.index.title'), followers_path(@profile), class: 'tab tab-bordered'%>
  <%= link_to t('workspaces.favorites.index.title'), favorites_path(@profile), class: 'tab tab-bordered'%>
</div>
<div class="container mx-auto" >
  <%= render "form", profile: @profile %>
  <%= link_to 'back', :back ,class: 'btn btn-primary'%>
</div>
<div class="tabs container mx-auto">
  <%= link_to t('workspaces.index.title'), workspace_path(@workspace), class: 'tab tab-bordered' %>
  <%= link_to t('workspaces.profile.title'), workspace_profile_path(@workspace), class: 'tab tab-bordered' %>
  <%= link_to t('workspaces.followings.index.title'), followings_path(@workspace), class: 'tab tab-bordered'%>
  <%= link_to t('workspaces.followers.index.title'), followers_path(@workspace), class: 'tab tab-bordered'%>
  <%= link_to t('workspaces.favorites.index.title'), favorites_path(@workspace), class: 'tab tab-bordered tab-active'%>
</div>
<div class="container mx-auto grid grid-cols-2 md:grid-cols-4 gap-4">
  <%= render partial: "projects/project", collection: @workspace.favorite_projects, as: "project" %>
</div>
